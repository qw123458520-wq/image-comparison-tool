# 文件管理 - 打包说明

## ✅ 配置已完成

打包配置已经全部设置好，可以立即开始打包！

---

## 📦 打包方法

### macOS 打包（当前系统）

```bash
# 打包成 .dmg 和 .zip（支持 Intel 和 Apple Silicon）
npm run electron:build:mac
```

**生成文件位置：** `release/`
- `文件管理-1.0.0-mac-x64.dmg` (Intel Mac)
- `文件管理-1.0.0-mac-arm64.dmg` (Apple Silicon)
- 对应的 .zip 文件

**首次打包时间：** 约 3-5 分钟

---

### Windows 打包

```bash
# 打包成 .exe 安装程序和 .zip
npm run electron:build:win
```

**注意：**
- 在 macOS 上为 Windows 打包需要安装 Wine
- 推荐在 Windows 系统上打包 Windows 版本

---

### Linux 打包

```bash
# 打包成 AppImage 和 .deb
npm run electron:build:linux
```

---

### 打包所有平台（通用命令）

```bash
npm run electron:build
```

这会根据当前系统打包对应平台的版本。

---

## 🎯 打包后如何分发

### 1. 本地分发
直接将 `release/` 目录中的安装包发送给用户：
- **macOS**: 发送 `.dmg` 文件
- **Windows**: 发送 `.exe` 安装程序
- **Linux**: 发送 `.AppImage` 文件（无需安装，直接运行）

### 2. 云盘分发
上传到：
- 百度网盘
- 阿里云盘
- Google Drive
- OneDrive

### 3. GitHub Releases
如果项目托管在 GitHub：
1. 创建新 Release
2. 上传打包文件
3. 添加版本说明

---

## 📝 版本管理

修改版本号：编辑 `package.json` 中的 `version` 字段

```json
{
  "version": "1.0.0"  // 修改这里
}
```

版本格式：`主版本.次版本.修订号`
- 主版本：重大更新
- 次版本：新功能
- 修订号：Bug 修复

---

## 🎨 添加应用图标（可选）

当前配置使用系统默认图标。如需自定义图标：

1. **准备图标文件**（512x512px 的 PNG）
2. **转换格式：**
   - macOS: 转换为 `.icns` → 放在 `build/icon.icns`
   - Windows: 转换为 `.ico` → 放在 `build/icon.ico`
   - Linux: 使用 `.png` → 放在 `build/icon.png`

3. **取消注释配置**（在 package.json 中添加图标路径）

**在线转换工具：**
- PNG → ICNS: https://cloudconvert.com/png-to-icns
- PNG → ICO: https://cloudconvert.com/png-to-ico

---

## ⚙️ 关键配置说明

### better-sqlite3 原生模块处理

已配置 `asarUnpack` 确保原生模块正确打包：
```json
"asarUnpack": [
  "node_modules/better-sqlite3/**/*"
]
```

### 平台支持

- **macOS**: x64 (Intel) + arm64 (Apple Silicon)
- **Windows**: x64 (64位)
- **Linux**: x64 (64位)

### 安装包类型

- **macOS DMG**: 拖拽安装，符合 macOS 习惯
- **Windows NSIS**: 标准安装向导，可自定义安装路径
- **Linux AppImage**: 无需安装，直接运行（类似绿色软件）

---

## 🔍 打包后测试

1. **安装测试**
   - 双击安装包
   - 完成安装流程
   - 启动应用

2. **功能测试**
   - 加载图片
   - 标注功能
   - 数据库保存
   - 导出报告

3. **卸载测试**
   - macOS: 从应用程序删除
   - Windows: 通过控制面板卸载
   - Linux: 删除 AppImage 文件

---

## 🐛 常见问题

### Q1: 打包时提示缺少依赖
```bash
npm install
npx electron-rebuild
npm run electron:build:mac
```

### Q2: macOS 提示"无法打开，因为来自身份不明的开发者"
**临时解决：** 右键点击应用 → 选择"打开"

**永久解决：** 申请 Apple 开发者证书并签名（需要 $99/年）

### Q3: Windows SmartScreen 拦截
这是正常现象（未签名应用）。用户可以点击"更多信息" → "仍要运行"

**解决方法：** 购买代码签名证书

### Q4: 打包文件太大
**当前大小：** 约 150-200MB（包含 Electron + Node.js + Chromium）

**这是正常的：**
- Electron 应用包含完整运行环境
- 无需用户安装额外依赖
- 保证跨平台一致性

---

## 📊 文件大小参考

- **macOS .dmg**: ~150MB
- **Windows .exe**: ~120MB
- **Linux AppImage**: ~140MB

---

## 🚀 首次打包步骤总结

```bash
# 1. 确保依赖已安装
npm install

# 2. 重新编译原生模块
npx electron-rebuild

# 3. 开始打包（macOS）
npm run electron:build:mac

# 4. 等待完成（3-5分钟）

# 5. 查看生成的文件
ls -lh release/
```

---

## ✨ 更新作者信息（可选）

编辑 `package.json`：
```json
{
  "author": "Your Name",  // 修改为你的名字
  "description": "专业的图像对比和标注工具..."  // 可自定义
}
```

---

## 📮 需要帮助？

如有问题，可以：
1. 查看 electron-builder 官方文档
2. 检查打包日志中的错误信息
3. 确认所有依赖已正确安装

**祝打包顺利！🎉**
